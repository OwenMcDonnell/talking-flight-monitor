<Window x:Class="tfm.firstRunHelp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tfm"
        mc:Ignorable="d"
        Title="firstRunHelp" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" >
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="txtFirstRun" Grid.Column="0" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" Height="Auto" Width="Auto" IsReadOnlyCaretVisible="True"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <CheckBox Content="Show this dialog on startup" IsChecked="{local:SettingBinding ShowFirstRunDialog}"/>
            <Button x:Name="btnOk" Content="Ok" Click="btnOk_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
